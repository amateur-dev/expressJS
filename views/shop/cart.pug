extends ../layouts/main-layout.pug

block styles
    link(rel="stylesheet", href="/css/product.css")

block content
    main
        h1 Working on this
        //- if products.length > 0
        //-     ul
        //-         each product in products
        //-             li
        //-                 p products.title p.qty
        //-                 form(action="/cart-delete-item", method="POST")
        //-                     input(type="hidden" value=product.prodID)
        //-                     label(for="title") Title
        //-                     input(type="text", name="title")#title
        //-                     label(for="price") Price
        //-                     input(type="number", name="price" min="0" step="0.01")#price
        //-                     label(for="imageUrl") Image URL
        //-                     input(type="url", name="imageUrl" min=0)#image
        //-                     label(for="description") Description
        //-                     textarea(type="text", name="description")#description
        //-                 button.btn(type="submit") Add Product
        //-             article.card.product-item
        //-                 header.card__header
        //-                     h1.product__title #{product.title}
        //-                 div.card__image
        //-                     img(src=product.imageUrl    , alt="A Book")
        //-                 div.card__content
        //-                     h2.product__price $#{product.price}
        //-                     p.product__description #{product.description}
        //-                 .card__actions
        //-                     a.btn(href="/product/" + product.id) Details
        //-                     include ../includes/addToCart.pug
        //- else
        //-     h1 No Products
        